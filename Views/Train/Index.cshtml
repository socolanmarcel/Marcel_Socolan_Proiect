@model IEnumerable<Marcel_Socolan_Proiect.Models.Train>

@{
    ViewData["Title"] = "Toate trenurile";
}

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">Toate trenurile</h2>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="Train" asp-action="Create" class="btn btn-primary">
                <i class="bi bi-plus-lg"></i>
                &nbsp;
                Adauga un nou tren
            </a>
        </div>
    </div>

    <br/>

    <table class="table table-bordered table-stripped" style="width:100%">
        <thead>
            <tr>
                <th>@Html.DisplayNameFor(model => model.Speed)</th>
                <th>@Html.DisplayNameFor(model => model.Capacity)</th>
                <th></th>
                <th></th>
            </tr>
        </thead>
        <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Speed)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Capacity)
            </td>
            <td>
                <div class="w-75 btn-group" role="group">
                    <a asp-controller="Train" asp-action="Edit" asp-route-id="@item.Id" class="btn btn-primary mx-2">
                        <i class="bi bi-pencil"></i>
                        Editeaza
                    </a>
                </div>
            </td>
            <td>
                <div class="w-75 btn-group" role="group">
                    <a asp-controller="Train" asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger mx-2">
                        <i class="bi bi-trash"></i>
                        Sterge
                    </a>
                </div>
            </td>
        </tr>
}
        </tbody>
    </table>
</div>
